syntax = "proto3";

package user;

option go_package = "./user";


message response {
  int32 code = 1;
  string  msg = 2;
}

//登录
message loginRes {
  string  username = 1;
  string password = 2;
}

//用户信息
message info {

}


service users {
  rpc login(loginRes) returns(response);
}
